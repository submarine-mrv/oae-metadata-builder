# Deprecated Components

This folder contains components that are no longer actively used but are kept for reference or potential future use.

## SpatialCoverageFlatField.tsx

- **Status**: Unused
- **Alternative**: `SpatialCoverageMiniMap` (active component with map modal)
- **Reason**: Alternative flat text-input version of spatial coverage that was created but never used in production
