# GA4 measurement ID, shared with oae-data-web (same Data Stream; the two
# subdomains are split in the GA UI by the Hostname dimension).
#
# Leave empty to disable analytics. The real value is set in Netlify under
# Site settings -> Environment variables.
VITE_GA_MEASUREMENT_ID=
